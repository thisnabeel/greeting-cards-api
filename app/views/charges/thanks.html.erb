<style>
	.jumbotron {
		background-color: #fff;
		border: 1px solid #000;
		text-align: center;
	}

	.emoji {
		font-size: 70px;
	}

	.shopfront {
		max-width: 350px;
		margin: 0 auto;
	}

	.status {
		padding: 10px;
		border-radius: 10px;
		background-color: #c0392b;
		color: #fff;
	}
</style>


<div class="shopfront">
		
	<img src="https://i.giphy.com/media/l3q2wJsC23ikJg9xe/200.webp" alt="">

	<p class="emoji">
		👏👏👏
	</p>
	<h2>We'll call when your order is ready for pickup!</h2>
	<small>Question/Concern? Call Us At: <a href="tel:510-509-8861">510-509-8861</a></small><br>
	<br>

	<div class="invoice">
		<div class="status"><i class="fa fa-times-circle"></i> 
			<span>Not Yet Ready</span>
		</div>
		<br>
		<b>Order Placed: <%= @sale.created_at.strftime("%a, %d %b %Y %l:%M %p") %></b>
		<h1>Paid: $<%= @sale.amount %></h1>
		<% if @sale.product.present? %>
			<h2>
				<a href="/products/<%= @sale.product.id %>">
					<%= @sale.product.title %> => <%= @sale.quantity %>
				</a>
			</h2>
		<% end %>
		<% if @sale.email.present? %>
		    <hr>
			<h3>Email:</h3>
			<p><%= @sale.email %></p>
		<% end %>
		<% if @sale.phone_number.present? %>
		    <hr>
			<h3>Phone Number:</h3>
			<p><%= @sale.phone_number %></p>
		<% end %>
		<% if @sale.requests.present? %>
		    <hr>
			<h3>Requests:</h3>
			<p><%= @sale.requests %></p>
		<% end %>
	</div>


</div>